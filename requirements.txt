numpy>=1.24.0      # Required for efficient, bounded-memory array operations and FFT calculations
scipy>=1.10.0      # Required for numerical integration (e.g., RK4) in the physics simulator
boto3>=1.28.0
pytest>=7.4.0 